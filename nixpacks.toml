[phases.setup]
aptPkgs = ["libpango-1.0-0", "libpangocairo-1.0-0", "libgdk-pixbuf2.0-0", "libffi-dev", "shared-mime-info", "libcairo2", "libglib2.0-0"]

[phases.install]
cmds = ["pip install -r requirements.txt"]
